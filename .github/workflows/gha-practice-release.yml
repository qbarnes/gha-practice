name: gha-practice-release

on:
  push:
    branches:
      - master
    paths:
      - 'version.h'

#  label:
#    types:
#      - [created, edited]


#permissions:
#add this later


jobs:
  release:
    name: Make Release
    uses: ./.github/workflows/gha-practice-build.yml
    secrets: inherit
    runs-on: ubuntu-latest
    needs: build_all
    steps:
#      - uses: actions/checkout@v3
#      - name: build tar files
#        run: make fullrelease
      - run: echo ${{ needs.build_all.outputs.build_version }}
      - name: Upload artifact
        uses: actions/upload-artifact@v3
        with:
          path: ./gha-practice-*.tar.gz
          if-no-files-found: error
